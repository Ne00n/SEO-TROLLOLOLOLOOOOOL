# Copy this file to .env and fill in your credentials and settings.
# The script reads environment variables from your shell; if you use a loader like python-dotenv, source them accordingly.
# STARTTLS is used for both IMAP (port 143) and SMTP (port 587).

# IMAP (STARTTLS)
IMAP_HOST=imap.example.com
IMAP_PORT=143
IMAP_USER=your-imap-username
IMAP_PASS=your-imap-password

# SMTP (STARTTLS)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password

# From address used for replies
MAIL_FROM=support@example.com

# Fetch limits and state
FETCH_LIMIT=50
STATE_FILE=state.json

# Keywords to trigger auto-reply when found in Subject (case-insensitive)
KEYWORDS=urgent,asap,help,support,issue,error,problem

# Ollama configuration
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1:latest
OLLAMA_TIMEOUT=60

# Business hours configuration
BUSINESS_HOURS_TZ=Europe/Berlin
BUSINESS_DAYS=Mon-Fri
BUSINESS_START=09:00
BUSINESS_END=17:00

# Out-of-hours standard message
OUT_OF_HOURS_MESSAGE=Thank you for your email. Our team is currently outside business hours and will get back to you during our next working period.